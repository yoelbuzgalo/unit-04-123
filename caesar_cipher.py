def encrypt_letter(letter, shift):
    # Convert letter to ascii using ord
    # Shift new letter by adding shift value to the ascii key
    # Convert the new letter ascii key back to character form
    # Return the converted letter